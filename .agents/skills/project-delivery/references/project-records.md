# Project records

`PROJECT.md` defines intent; `.project/data.md`, `.project/architecture.md`, and `.project/evaluation.md` define governed inputs; `DESIGN.md` defines the user-facing contract; `.project/milestones.yml`, `.project/approvals.yml`, `.project/evidence.yml`, `.project/state.md`, and `.project/handoff.md` control delivery; `.project/publication.md`, `portfolio/project.json`, and `CASE-STUDY.md` control public claims. Update only records affected by verified facts.
